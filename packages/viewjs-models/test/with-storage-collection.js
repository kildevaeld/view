const Model = require('../lib/model').Model,
    withStorageModel = require('../lib/with-storage-model').withStorageModel,
    withStorageCollection = require('../lib/with-storage-model').withStorageCollection,
    {
        ModelEvents
    } = require('../lib/types'),
    {
        createError,
        ModelErrorCode
    } = require('../lib/errors'),
    sinon = require('sinon'),
    Storage = require('./fixtures/storage').Storage;


describe('withStorageCollection', () => {

    it('', () => {

    });

});